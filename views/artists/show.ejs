<% include ../partials/header %>


    <div class="row">
        
        <div class="col-md-3">
            
            <div class="list-group">
                <li class="list-group-item active">Info 1</li>
                <li class="list-group-item">Info 2</li>
                <li class="list-group-item">Info 3</li>
            </div>
        </div>
        
        <div class="col-md-9">
            <div class="thumbnail"><img class="img-responsive" src="<%= artist.logo %>">
                <div class="caption-full">
                    
                    <h4><a href="#"><%= artist.name %></a></h4>
                    <p><%= artist.blurb %></p>
                    <p><em>Added by <%= artist.author.username %>, <%= moment(artist.createdAt).fromNow() %></em></p>
                    <% if(currentUser && artist.author.id.equals(currentUser._id) || currentUser && currentUser.isAdmin){ %>
                        <a class="ui primary button" href="/artists/<%= artist._id %>/edit">Edit Artist</a>
                        <form class="delete-form" action="/artists/<%= artist._id %>?_method=DELETE" method="POST">
                            <button class="ui negative button">Delete</button>
                        </form>
                    <% }; %>
                </div>
            </div>
            
            
        </div>
            
    </div>

    


<% include ../partials/footer %>

